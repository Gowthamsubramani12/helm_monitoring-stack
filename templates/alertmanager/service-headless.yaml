apiVersion: v1
kind: Service
metadata:
  name: prometheus-alertmanager-headless
  namespace: monitoring
  labels:
    app: prometheus
    component: alertmanager
spec:
  clusterIP: None
  ports:
    - port: 9093
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: prometheus
    component: alertmanager
